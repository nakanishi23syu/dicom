{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Dicom": "Data Source=dicom.db"
  },
  "Cors": {
    // 本番等では環境ごとに実際のフロントエンドのオリジンを指定する（既定は空＝全拒否）。
    "AllowedOrigins": []
  }
}
