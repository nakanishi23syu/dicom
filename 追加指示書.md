たくさんの指示をする。量が多いのでサブエージェントで作ってほしい。


# 正式にバックエンドを追加してほしい。
- 技術スタック
  - C#
  - Hot Chocolate
  - Entity フレームワーク
    - BananaCakePopも使えるようにしたい。
  - GraphQL
  - Sqlite
- バックエンドを追加するにあたってプロジェクトを2repozitori構成にしてほしい。
- フロントとバックでフォルダを分けて、その親フォルダにclaude関係やドキュメント関係を配置してほしい。
- これを参考に他のプロジェクトの環境構築するので、環境構築手順書を作ってほしい。
- バックはC#なのでエディタはVisualStudioを利用する

# テーブル
- 基本は、user_study, user_series, user_sopに分ける
- 毎回dicomのメタデータを参照するのが面倒なので、実装で扱う個所(患者名、ID、検査日、部位等)をカラムとして外だししてほしい。
- 必用ならindexも貼ってほしい

# 汎用コンポーネント
- 保存ボタン、キャンセルボタン、通知ポップアップ(モーダル)　yes noポップアップなど、汎用コンポーネントを作りたい
- slotで外から文言を入れらえるように、押下時のイベント登録等ができるようにしたい。

# css周りをきちんとしたい。
- globalでUIの色を変えられるようにしたい。
- bem等の設計はやらなくていいので、一気に見た目変えられるようにしたい。
- 全体的にcssに開設コメントを付けてほしい。私はcss初心者

# 定数定義ファイル等を作りたい
.envやconstを定義するファイルを作ってほしい。


# 以下の機能を追加してほしい。
- チュートリアル機能追加
  - ref toref reactive等の参照保持系の使い方、違い説明
- 検査・モーダル・sop一覧にNotionのような並べ替え機能を付けたい
  - 並び替えにはライブラリを使用しない。htmlのdragable 等を使って実装してほしい。
  - 3つの並べ替え処理があるのでcomposableを作って、共通化をしてほしい。
  - 並び替え保存ボタンを押すと、各テーブルのorderカラムに順番が記録される。
  - 並び替え適用ボタンで並び替えを適用する
- dicom画像はバックエンド側の適切な場所に入れてほしい(Vue側ではない)
- dicom画像のアップロード機能を作ってほしい
  - ドラッグ＆ドロップでファイル選択、フォルダ一括選択、ファイル一括選択、参照選択等、レパートリーを設けてほしい
  - アップロードボタンを押すと、バックエンド側の各テーブルにデータがインサートされて、dicom画像もバック側のストレージ管理フォルダに入る
- ソート・フィルター機能
  - 各studyに対して、Notionのようにフィルター・ソートできる機能が欲しい。
- その他、あった方がいい機能、SYNAPSE LEADにある機能はたくさん入れてほしい。
- 機能追加でテーブルやカラムをつくってもいい
- JWTで認証機能を作ってほしい
  - 医者が最初にログインすると、管理者アカウントなら、できることが増える